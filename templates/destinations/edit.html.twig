{% extends 'base.html.twig' %}

{% block title %}Edit Destinations{% endblock %}

{% block body %}
    <h1>Edit Destinations</h1>

    {{ include('destinations/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('destinations_index') }}">back to list</a>

    {{ include('destinations/_delete_form.html.twig') }}
{% endblock %}
